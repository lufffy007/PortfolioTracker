"""
`app` package

In .NET terms, you can think of this as the "Web API project".

We keep all backend code for FastAPI inside this folder:
- `main.py`     -> similar to `Program.cs` / startup entry point
- `core/`       -> similar to a `Configuration` or `Infrastructure` folder
- `routers/`    -> similar to ASP.NET "controllers"
- `external/`   -> wrappers/clients for external SDKs (here: SnapTrade)
"""

"""
Application package for the Portfolio Tracker backend.
"""

