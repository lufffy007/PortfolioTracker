"""
Core utilities and configuration for the FastAPI app.

In .NET you might think of this as:
- `appsettings.json` + `Options` pattern
- plus any cross-cutting infrastructure helpers.

Right now we only have configuration using environment variables.
"""

"""
Core application configuration, dependencies, and security utilities.
"""

